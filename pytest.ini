[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = --strict-markers
markers =
    collective: white-label studio (Collective overlay) regression — entitlements, brand, governance, billing
