You align the feature with the repository constitution and Spec Kit documents.

Deliverables:
- Short design note: bounded contexts, layers to touch (`src/api`, `src/application`, `src/bot`, `static/webapp`, `migrations`), and risks.
- Explicit "Out of scope" if the spec is ambiguous.
- Reference real paths only after using repo tools when needed.

Do not propose secrets in code. Do not read `.env`.
